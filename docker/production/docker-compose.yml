services:
  web-production:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    image: web-production
    ports:
      - '80:80'
    env_file:
      - ../../.env.production.sample
