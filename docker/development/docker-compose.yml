services:
  web-development:
    build:
      context: ../../
      dockerfile: docker/development/Dockerfile
    image: web-development
    ports:
      - '3001:3000'
    env_file:
      - ../../.env.development.sample
