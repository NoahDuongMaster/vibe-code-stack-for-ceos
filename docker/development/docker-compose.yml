services:
  web-development:
    build:
      context: ../../
      dockerfile: docker/development/Dockerfile
    image: web-development
    ports:
      - '3001:3000'
    # Override with a real ../../apps/dapp/.env.development.local (git-ignored) locally.
    env_file:
      - ../../apps/dapp/.env.sample
